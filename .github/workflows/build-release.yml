name: Build Windows release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Integrate vcpkg (manifest mode restores sqlite3 and libpq on build)
        run: '& "$env:VCPKG_INSTALLATION_ROOT\vcpkg.exe" integrate install'

      - name: Build (x64 Release)
        shell: pwsh
        run: |
          $sdk = (Get-ChildItem 'C:\Program Files (x86)\Windows Kits\10\Include' |
            Sort-Object Name -Descending | Select-Object -First 1).Name
          Write-Host "Using Windows SDK $sdk"

          msbuild build\vs2017\pingstats.sln `
            /m `
            /p:Configuration=Release `
            /p:Platform=x64 `
            /p:PlatformToolset=v143 `
            /p:WindowsTargetPlatformVersion=$sdk

          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Package release zip
        shell: pwsh
        run: |
          # bin\ also holds sqlite3.dll, libpq.dll and everything libpq
          # pulls in, copied there automatically by vcpkg's AppLocal
          # deployment step during the build.
          Compress-Archive -Path bin\* -DestinationPath pingstats-windows-x64.zip -Force

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: pingstats-windows-x64
          path: pingstats-windows-x64.zip

      - name: Attach zip to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: pingstats-windows-x64.zip
          generate_release_notes: true
